# Core — no Anthropic SDK needed, megamind uses the claude CLI directly
pyyaml>=6.0
python-dotenv>=1.0
rich>=13.0
textual>=0.80.0
typer>=0.12.0
sqlite-utils>=3.36

# Memory — local mode (default, zero services)
numpy<2                      # sentence-transformers torch compat
sentence-transformers>=3.0

# Memory — cloud mode (optional, set memory_mode: cloud in player.yaml)
# mem0ai>=0.1.0
